커밋 규칙을 준수하면서 git commit 을 수행합니다.

## 커밋 메시지 형식

```text
{타입}: {제목}

- 추가 설명 1
- 추가 설명 2
```

- 타입과 콜론 사이 공백 없음, 콜론 뒤 공백 1개
- 제목과 본문 사이 빈 줄 1개
- 본문은 `-` 로 시작하는 bullet point (선택사항)
- 언어: 한국어 권장, 기술 용어·고유명사는 영어 사용
- 이모지 사용 금지
- 제목은 "무엇을 변경했는가" 를 쓴다. 변경의 동기(리뷰 반영, 요청사항 등)는 제목이 아닌 본문에 적는다
  - bad: `refactor: 코드리뷰 반영`
  - good: `refactor: 예외 응답에서 내부 메시지 노출 제거`

## 본문에 담을 맥락

본문은 단순히 "무엇을 했는가" 만 적지 말고, **그 결정에 어떻게 도달했는가** 까지 담는다. 미래의 리뷰어·자기 자신이 commit 한 개로도 PR description 처럼 맥락을 잡을 수 있어야 한다.

담을 만한 결정 흐름:
- 선택지 사이에서 어느 쪽을 택한 이유
- 처음 시도 → 전환한 흐름
- 작업 중 발견한 함정·누락을 정정한 부수효과

**할루시네이션 금지.** 실제로 대화·작업에서 일어난 결정·트레이드오프·발견만 적는다. 추측·미래 의도·"AI 가 도왔다" 같은 메타정보는 적지 않는다.

예시:

```text
feat: 공통 응답 래퍼·예외 체계·전역 예외 핸들러

- ErrorCategory 가 HttpStatus 를 소유하게 해 예외가 status 를 직접 들지 않도록 함
- 로그 레벨은 4xx info / 5xx warn 으로 갈랐다. 규약의 "info" 는 클라이언트 계약 위반을
  가리킨 것으로 보고, 502 를 info 로 묻으면 외부 장애를 놓치기 때문
```

## 허용 타입

commit-msg 훅이 기계 강제한다. 훅의 **정본(SSOT)은 이 레포의 `.githooks/commit-msg`** 이고, SessionStart 훅(`.claude/settings.json`)이 세션 시작마다 `core.hooksPath` 를 그 디렉터리로 맞춘다.

이 문서는 타입 목록을 열거하지 않는다 — 열거하면 정본과 두 곳이 되어 어긋난다. **현재 허용 타입은 `.githooks/commit-msg` 를 읽어 확인**하고, 바꿀 땐 그 정본을 고친다 (이 문서가 아니라).

분류 라벨(`/issue` 가 부여)과 커밋 타입은 대부분 1:1 이지만 완전히 같지는 않다 — `epic` 라벨엔 커밋이 없고, `style` 타입엔 라벨이 없다.

## 커밋 분리 원칙

- 타입이 다르면 반드시 분리 (예: `chore` 설정 + `feat` 기능 → 커밋 2개)
- 같은 타입이라도 논리적으로 독립된 작업이면 분리
- 분리 시 의존 순서대로 커밋 (예: 설정 → 기능 → 테스트)

## 제외 대상

- **작업 중 흘러나온 임시·스크래치 .md** (작업 메모, 디버그 기록, 계획 scratchpad 등)는 커밋하지 않는다 — 사용자가 명시적으로 요청한 경우는 제외.
- **의도적으로 작성·수정한 문서**(`CLAUDE.md`·`README`·`docs/` 등)는 `docs:` 로 정상 커밋한다. .md 확장자라는 이유만으로 막지 않는다.
- 헷갈리면 "이 .md 가 이번 작업의 산출물인가, 작업하다 흘린 부산물인가" 로 가른다. 산출물이면 커밋, 부산물이면 제외.
- `.gradle/`·`build/`·`.idea/` 등 `.gitignore` 대상이 untracked 로 보이면 커밋 대상이 아니다.

## 절차

1. `git status` 와 `git diff HEAD` 로 전체 변경 내역 확인 (staged + unstaged 모두 포함)
2. 제외 대상 파일을 걸러낸 뒤, 커밋 대상이 없으면 커밋을 중단하고 "커밋 대상 파일이 없습니다" 를 안내한다
3. 커밋 대상이 있으면 커밋 분리 원칙에 따라 변경 내역을 논리적 단위로 분류
4. 각 단위별로 **해당 파일만 경로로 지정해** `git add` → 커밋 메시지 작성 → 커밋
   - **`git add .` · `git add -A` 는 쓰지 않는다.** 작업 디렉터리에는 커밋 대상이 아닌 것이 늘 함께 있다(일일 리포트·스크래치·다른 작업의 미완성 변경). 실제로 기능 PR 에 일일 리포트 5개가 섞여 들어간 적이 있다.
5. 모든 커밋 완료 후 `git log` 로 확인

$ARGUMENTS
