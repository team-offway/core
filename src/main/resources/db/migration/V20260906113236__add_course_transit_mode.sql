-- 저장 코스가 "무엇을 타고 가는지" 를 기억한다(#456).
--
-- #455 가 생성에 transitMode 를 열었지만, 그 값을 정하는 칩은 저장 코스 상세에 있고 그 화면은
-- 생성 API 를 부르지 않는다. 결과가 아니라 입력을 저장해 두고 상세에서 계산하는 #423 의 방식대로,
-- 수단도 입력으로 보고 여기에 담는다 — 다시 열어도 같은 수단이라 화면이 흔들리지 않는다.
--
-- NULL 이 정상이다. 이 컬럼 이전에 저장된 코스와 자차 코스가 그렇고, NULL 이면 서버가 고른다.
ALTER TABLE course ADD COLUMN transit_mode VARCHAR(20) NULL;
