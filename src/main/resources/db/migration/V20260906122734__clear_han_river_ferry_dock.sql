-- 한강 나루를 여객선 항구에서 뺀다(#454) — #452 가 채운 것을 되돌린다.
--
-- `여의도` 는 TAGO 여객선 목록에 있지만 실제로는 **한강 유람선 선착장**(진성나루)이다. #452 의 재지오코딩
-- 규칙이 이름에 '나루' 가 있으면 항구로 인정했고, 그래서 좌표가 서울 영등포구로 채워졌다.
--
-- **빈 것보다 나쁘다.** 서울역에서 4.3㎞ 라 출발 항구 탐색(반경 20㎞)에 걸린다. 그 결과 서울에서
-- 완도·하동을 물으면 "여의도에서 배를 타세요" 가 나왔다 — 한강에서 완도로 가는 배는 없다.
-- 좌표가 비어 있을 때는 "어디서 타는지 모름" 이라 적어도 틀리지는 않았다.
--
-- 도착 쪽으로도 쓸 일이 없다. 인구감소지역 89곳 중 여의도 반경 안에 있는 곳이 없다.
--
-- 되돌리는 방식은 #452 가 확인 못 한 항구들에 쓴 것과 같다 — 틀린 좌표는 엉뚱한 답을 만들지만,
-- 빈 좌표는 최근접 탐색에서 빠질 뿐이다.
UPDATE ferry_port SET lat = NULL, lng = NULL WHERE code = 'SEA97670';
