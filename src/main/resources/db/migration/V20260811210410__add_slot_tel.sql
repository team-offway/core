-- 슬롯에 대표 전화를 싣는다(#157).
--
-- 추가 외부 호출이 없다. 후보 조회(areaBasedList2) 응답에 이미 들어 있는데 후보 DTO 가 안 들고 와서
-- 버리고 있었고, 인허가 장소는 49% 가 전화를 갖고 있다. 코스 타임라인에서 장소를 누르지 않고도
-- 전화를 걸 수 있어야 한다.
--
-- 표시 정보라 없을 수 있다(nullable).

ALTER TABLE slot ADD COLUMN tel VARCHAR(40);
