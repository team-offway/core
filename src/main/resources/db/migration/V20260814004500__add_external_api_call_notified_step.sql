-- 어느 단계까지 알렸는지(#257).
--
-- record() 는 외부 호출마다 돈다. "70% 넘었다" 로 판정하면 그 뒤 모든 호출이 알림을 쏘므로,
-- "어디까지 알렸나" 를 어딘가 기억해야 한다.
--
-- 인메모리 플래그로 두지 않는 이유는 이 테이블을 애초에 DB 로 둔 이유와 같다 — 재배포마다 리셋돼
-- 이미 보낸 단계를 다시 보낸다. 배포가 잦은 날일수록 시끄러워지는, 정확히 반대 방향의 오차다.
--
-- 이 컬럼이 있으면 조건부 UPDATE(notified_step < :step)로 단계를 선점할 수 있어, 인스턴스가 여럿이어도
-- 한 단계에 한 번만 나간다. call_date 가 키라 자정을 넘기면 새 행이 되어 단계도 자연히 리셋된다.

ALTER TABLE external_api_call
    ADD COLUMN notified_step TINYINT NOT NULL DEFAULT 0;
